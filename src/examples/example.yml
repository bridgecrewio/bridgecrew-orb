description: |
  Scan repository with bridgecrew to find security vulnerabilities in your IaC files
usage:
  version: 2.1
  orbs:
    bridgecrew: bridgecrew/bridgecrew@0.0.5
  jobs:
    build:
      machine: true
      steps:
        - bridgecrew/scan:
            directory: "."
            soft-fail: true
            output: "json"
            repo-id: "bridgecrewio/platform"
            api-key: "6bridgecrew-9example-2api-key1-demo9"
